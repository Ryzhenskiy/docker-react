os: linux
dist: focal

services:
  - docker

before_install:
  - docker build -t ryzhenskiy/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ryzhenskiy/docker-react npm test -- --watchAll=false
